apiVersion: v1
kind: Service
metadata:
  name: {{ include "gateforge.fullname" . }}-frontend
  labels:
    app: gateforge-frontend
    {{- include "gateforge.labels" . | nindent 4 }}
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: gateforge-frontend
