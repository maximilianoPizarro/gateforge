{{- if .Values.datagrid.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "gateforge.datagrid" . }}
  labels:
    {{- include "gateforge.datagridLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 11222
      targetPort: hotrod
      protocol: TCP
      name: hotrod
  selector:
    {{- include "gateforge.datagridSelectorLabels" . | nindent 4 }}
{{- end }}
